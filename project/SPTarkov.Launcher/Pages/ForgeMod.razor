@page "/ForgeMod/{ModId}"

@implements IDisposable

@inject HttpHelper HttpHelper
@inject ILogger<ForgeMod> Logger
@inject TitleHelper TitleHelper
@inject LocaleHelper LocaleHelper
@inject StateHelper StateHelper
@inject NavigationManager NavigationManager

<MudContainer Class="px-4 mt-2 mb-6" MaxWidth="MaxWidth.Large">
    <MudGrid>
        @if (SearchFailed)
        {
            <MudItem sm="12">
                <MudPaper Elevation="2" Class="pa-4 border-2 border-solid mud-border-lines-default card-container">
                    <MudContainer Class="d-flex flex-column align-center gap-2 py-12">
                        <MudIcon Icon="@Icons.Material.Filled.ErrorOutline" Style="font-size: 4rem;" Class="mud-text-secondary"/>
                        <MudText Typo="Typo.h6" Align="Align.Center">@(LocaleHelper.Get("forge_error_title"))</MudText>
                        <MudText Align="Align.Center" Class="mud-text-secondary">@(LocaleHelper.Get("forge_search_failed"))</MudText>
                        <MudButton Size="Size.Small" Variant="Variant.Filled" Color="Color.Primary" StartIcon="@Icons.Material.Filled.Refresh" OnClick="LoadMod" Class="mt-2">@(LocaleHelper.Get("forge_error_retry"))</MudButton>
                    </MudContainer>
                </MudPaper>
            </MudItem>
        }
        else if (!SearchInProgress && SearchResponse?.Data is { } mod && VersionResponse?.Data?.FirstOrDefault() is { } version)
        {
            <DetailedModComponent ForgeMod="@mod" ForgeModVersion="@version" ForgeModAddons="@(AddonResponse?.Data ?? [])"/>
        }
        else
        {
            <DetailedModComponent IsSkeleton="true"/>
        }
    </MudGrid>
</MudContainer>

@code {
    private ForgeModResponse? SearchResponse { get; set; }
    private ForgeVersionResponse? VersionResponse { get; set; }
    private ForgeAddonResponse? AddonResponse { get; set; }
    private bool SearchInProgress { get; set; }
    private bool SearchFailed { get; set; }
    private CancellationTokenSource? LoadCancellation { get; set; }

    [Parameter]
    public string ModId { get; set; } = "";

    protected override async Task OnParametersSetAsync()
    {
        if (!StateHelper.IsLocalServerSelected)
        {
            NavigationManager.NavigateTo("/ServerPageWrapper/false");
            return;
        }

        await LoadMod();
    }

    private async Task LoadMod()
    {
        LoadCancellation?.Cancel();
        LoadCancellation?.Dispose();
        LoadCancellation = new CancellationTokenSource();
        var loadToken = LoadCancellation.Token;
        var modId = ModId;

        SearchResponse = null;
        VersionResponse = null;
        AddonResponse = null;
        SearchFailed = false;

        await SearchForMod(modId, loadToken);

        if (loadToken.IsCancellationRequested)
        {
            return;
        }

        if (SearchResponse?.Data is null)
        {
            SearchFailed = true;
            return;
        }

        await SearchForLatestVersion(modId, loadToken);

        if (loadToken.IsCancellationRequested)
        {
            return;
        }

        if (VersionResponse?.Data?.FirstOrDefault() is null)
        {
            SearchFailed = true;
            return;
        }

        await SearchForModAddons(modId, loadToken);
    }

    public void Dispose()
    {
        LoadCancellation?.Cancel();
        LoadCancellation?.Dispose();
        LoadCancellation = null;
    }

    private static CancellationTokenSource CreateTimeoutSource(CancellationToken loadToken)
    {
        var source = CancellationTokenSource.CreateLinkedTokenSource(loadToken);
        source.CancelAfter(TimeSpan.FromSeconds(10));
        return source;
    }

    private async Task SearchForLatestVersion(string modId, CancellationToken loadToken)
    {
        try
        {
            using var token = CreateTimeoutSource(loadToken);
            var response = await HttpHelper.ForgeGetLatestCompatibleModVersion(modId, token.Token);

            if (loadToken.IsCancellationRequested)
            {
                return;
            }

            if (response?.Data?.Count > 0)
            {
                VersionResponse = response;
                StateHasChanged();
                return;
            }
        }
        catch (OperationCanceledException) when (loadToken.IsCancellationRequested)
        {
            // Expected when navigating to another mod. Stale result is discarded.
            return;
        }
        catch (Exception e)
        {
            Logger.LogError("Exception thrown from ForgeHelper.SearchForLatestVersion {Exception}", e);
        }

        var firstVersion = SearchResponse?.Data?.Versions?.FirstOrDefault();
        if (firstVersion != null)
        {
            await SearchForModVersion(modId, firstVersion.Id.ToString(), loadToken);
        }
    }

    private async Task SearchForMod(string modId, CancellationToken loadToken)
    {
        SearchInProgress = true;

        try
        {
            using var token = CreateTimeoutSource(loadToken);
            var response = await HttpHelper.ForgeGetMod(modId, token.Token);

            if (loadToken.IsCancellationRequested)
            {
                return;
            }

            if (response is null)
            {
                Logger.LogError("Getting mod {modId} returned no response", modId);
                return;
            }

            SearchResponse = response;

            if (SearchResponse.Data is not null)
            {
                TitleHelper.Apply($"ForgeMod/{modId}", SearchResponse.Data.Name);
            }
        }
        catch (OperationCanceledException) when (loadToken.IsCancellationRequested)
        {
            // Expected when navigating to another mod. Stale result is discarded.
        }
        catch (Exception e)
        {
            Logger.LogError("Exception thrown from ForgeHelper.SearchForMod {Exception}", e);
        }
        finally
        {
            if (!loadToken.IsCancellationRequested)
            {
                SearchInProgress = false;
                StateHasChanged();
            }
        }
    }

    private async Task SearchForModVersion(string modId, string versionId, CancellationToken loadToken)
    {
        try
        {
            using var token = CreateTimeoutSource(loadToken);
            var response = await HttpHelper.ForgeGetModVersion(modId, versionId, token.Token);

            if (loadToken.IsCancellationRequested)
            {
                return;
            }

            if (response is null)
            {
                Logger.LogError("Getting mod version {modId}:{versionId} returned no response", modId, versionId);
                return;
            }

            VersionResponse = response;

            if (!VersionResponse.Success)
            {
                Logger.LogError("Getting Mod version failed {modId}:{versionId} for reason: {code}:{message}", modId, versionId, VersionResponse.Code, VersionResponse.Message);
            }
        }
        catch (OperationCanceledException) when (loadToken.IsCancellationRequested)
        {
            // Expected when navigating to another mod. Stale result is discarded.
        }
        catch (Exception e)
        {
            Logger.LogError("Exception thrown from ForgeHelper.SearchForModVersion {Exception}", e);
        }
        finally
        {
            if (!loadToken.IsCancellationRequested)
            {
                StateHasChanged();
            }
        }
    }

    private async Task SearchForModAddons(string modId, CancellationToken loadToken)
    {
        try
        {
            using var token = CreateTimeoutSource(loadToken);
            var response = await HttpHelper.ForgeGetModAddons(modId, token.Token);

            if (loadToken.IsCancellationRequested)
            {
                return;
            }

            if (response is null)
            {
                Logger.LogError("Getting mod addons {modId} returned no response", modId);
                return;
            }

            AddonResponse = response;

            if (!AddonResponse.Success)
            {
                Logger.LogError("Getting Mod Addons failed {modId}", modId);
            }
        }
        catch (OperationCanceledException) when (loadToken.IsCancellationRequested)
        {
            // Expected when navigating to another mod. Stale result is discarded.
        }
        catch (Exception e)
        {
            Logger.LogError("Exception thrown from ForgeHelper.SearchForModAddons {Exception}", e);
        }
        finally
        {
            if (!loadToken.IsCancellationRequested)
            {
                StateHasChanged();
            }
        }
    }
}
