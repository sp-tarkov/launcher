@page "/AdminPage"

@inject LogHelper LogHelper

<MudContainer Class="px-8 my-2 pa-0" MaxWidth="MaxWidth.Large">
    <MudPaper Class="mud-width-full pa-4 flex-column">
        @foreach (var log in LogHelper.GetLogs())
        {
            <MudText Typo="Typo.body1">
                @log
            </MudText>
        }
    </MudPaper>
</MudContainer>
